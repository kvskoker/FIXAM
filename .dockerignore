node_modules/
.git/
.gitignore
*.md
# Patterns without ** match only the top level of the build context, so a bare
# ".env" here did NOT exclude backend/.env -- and the simulator, which builds
# from the repo root, baked that file (real credentials) into its image.
**/.env
**/.env.*
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
coverage/
test/
graphify-out/
