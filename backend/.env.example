# Hugging Face API Token (required for gated models)
HF_TOKEN=your_huggingface_token_here

# Whisper Model Selection
# Choose based on your server's available RAM:
# - openai/whisper-base (default, ~140MB, requires ~500MB RAM)
# - openai/whisper-small (~460MB, requires ~1GB RAM)
# - openai/whisper-medium (~1.5GB, requires ~2GB RAM)
# - openai/whisper-large-v3-turbo (~1.5GB, requires ~2.5GB RAM)
WHISPER_MODEL=openai/whisper-base
